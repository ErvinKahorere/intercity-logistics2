InterCity Logistics upgrade pass

Implemented in this export:
- homepage now uses more dynamic backend-fed data for trust stats, live requests, and featured drivers
- added parcel preview endpoint for estimate + ranked driver preview before submitting
- upgraded homepage hero into a more product-like request wizard with live quote + top driver matches
- reduced footer copy and aligned branding more tightly
- refined global UI utility styles for fields, hero cards, and lighter layered surfaces

Notes:
- This pass focuses on stronger landing-page conversion and better first-use flow.
- There are still other screens that can be polished further, especially deeper dashboard/API state sync across the full app.
